<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.redhood.hoolicalendar">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".LaunchViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:launchMode="singleTask">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:launchMode="singleTop"/>
        <activity android:name=".ForgetPwdActivity" android:launchMode="singleTop"/>
        <activity android:name=".RegisterActivity" android:launchMode="singleTop"/>
        <activity android:name=".WebViewActivity" android:launchMode="singleTop"/>
        <activity android:name=".SettingActivity" android:launchMode="singleTop"/>
        <activity android:name=".MyMessageActivity" android:launchMode="singleTop"/>
        <activity android:name=".NewsListDetailActivity" android:launchMode="singleTop"/>
        <activity android:name=".MyInfoActivity" android:launchMode="singleTop"/>


    </application>

</manifest>